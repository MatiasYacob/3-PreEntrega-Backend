<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <title>Tickets</title>
</head>
<body>

<div class="container mt-4">
    <h2 class="mb-4">Tus Tickets</h2>

    {{#if tickets.length}}
        {{#each tickets}}
            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title"><p><strong>Codigo de Compra:</strong> {{this.code}}</p> </h5>
                    <p class="card-text">Fecha de compra: {{this.purchase_datetime}}</p>
                    <p class="card-text">Monto: ${{this.amount}}</p>
                    <!-- Otros campos del ticket que desees mostrar -->
                </div>
            </div>
        {{/each}}
    {{else}}
        <p>No hay tickets disponibles.</p>
    {{/if}}
</div>

<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

</body>
</html>


